export const EditorPopup = (day: string) => {
  console.log("editor props: ", day);
  return (
    <>
      <div className="editor__popup">editor popup</div>
    </>
  );
};

export default { EditorPopup };
